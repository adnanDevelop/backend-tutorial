/*const fs = require('fs');

fs.writeFileSync('index.text' , 'testing fs module');                    CREATE FILE
const readfile = fs.readFileSync('index.text')                           READ FILE
const renameFile = fs.renameSync('content.text' , 'defination.text')     RENAME FILE
const delFile = fs.unlinkSync('practice.text')                           DELETE FILE   
const append = fs.appendFileSync('index.text' , 'adding new content')    APPEND MORE CONTENT IN SAME FILE

console.log(renameFile);
*/
